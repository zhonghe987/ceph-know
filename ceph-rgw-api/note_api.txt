1,创建用户
key-type必须为s3

2，创建子用户
key-type必须为swift


3，同个用户创建存在的桶名依然为200

4，修改用户信息中，指定秘钥同时也指定access_key，指定access_key也要指定秘钥，该用户增加一对access_key和sec_key，不影响之前的存在的秘钥对。

5,桶名必须唯一，表现为不同的用户不能创建相同的桶名，同个用户创建创建两个相同的桶名可以，后者不覆盖前者的数据，但实际上只有一个桶。

6,s3 api 创建文件夹需要添加'/'，例如 wewe/

7，存在文件夹的桶删除不掉，相应的该用户也删除不掉。
